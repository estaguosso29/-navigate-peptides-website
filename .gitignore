# Local dev artifacts
.claude-flow/
.claude/
.DS_Store
node_modules/

# Local Docker dev environment (local-only, never commit)
docker/

# Database dumps at repo root. The prod snapshot
# (navigatepeptides_com.sql) contains real customer PII — order records,
# emails, addresses. It must never be committed. See CLAUDE.md.
/*.sql
/*.sql.gz

# Jetpack backup archives — same PII exposure as the SQL snapshot above,
# plus wp_users and MailPoet subscriber tables.
/jetpack-backup-*.tar.gz
/*.tar.gz

# Build / temp
/tmp/
*.swp
*.bak
